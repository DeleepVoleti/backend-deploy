pipeline {
    agent {
        label 'Agent-1'
    }
    options {
        timeout(time: 30, unit: 'MINUTES')
        disableConcurrentBuilds()
        ansiColor('xterm')
    }
    parameters{
        string( name: 'appVersion', defaultValue:'1.0.0', description:'this is the app version')
    }
    stages {
        stage('read the version'){
            steps{
                script{
                    echo appversion ooo is ${appVersion}
                }
            }
        }
   }
    post { 
        always { 
            deleteDir()
        }
    }
}